fastapi
uvicorn
openai
python-dotenv
supabase
pymupdf

# Parser dependencies (statement_parser.py)
pydantic>=2.0.0
pandas>=2.0.0
openpyxl>=3.1.0
httpx

# Invoice/proof parsing: image OCR (Tesseract). Requires the Tesseract binary
# installed on the system (set TESSERACT_CMD in .env on Windows if not on PATH).
pytesseract
Pillow
